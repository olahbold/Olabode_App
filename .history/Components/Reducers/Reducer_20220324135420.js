const initialState = {
    loading : 'false'
}

export default Reducer=n (state = initialState, { type }) => {
  switch (type) {

  case first:
    return { ...state, ...payload }

  default:
    return state
  }
}

